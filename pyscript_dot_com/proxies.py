from pyscript_dot_com.requests import request


def proxy(name, *args, **kwargs):
    """Call a PyScript.com proxy by name."""
    # TODO: We need to grab the URL of the app and
    # convert it to a proxy URL.
    return 1
